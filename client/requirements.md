## Packages
framer-motion | Smooth layout transitions and modal animations
lucide-react | Beautiful icons for the UI
date-fns | Formatting dates for display

## Notes
The backend uses in-memory storage (as per user request for "no database" feel, though tech stack uses standard patterns).
Images are stored as base64 strings in the `content` field or URLs.
The app is a single-page dashboard.
